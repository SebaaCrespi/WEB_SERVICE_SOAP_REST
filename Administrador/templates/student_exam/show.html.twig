{% extends 'base.html.twig' %}

{% block title %}StudentExam{% endblock %}

{% block body %}
    <h1>StudentExam</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student_exam.id }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ student_exam.note }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ student_exam.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_student_exam_index') }}">back to list</a>

    <a href="{{ path('app_student_exam_edit', {'id': student_exam.id}) }}">edit</a>

    {{ include('student_exam/_delete_form.html.twig') }}
{% endblock %}
